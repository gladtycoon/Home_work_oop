*Home_work_oop* - это проект на Python в рамках разработки ядра для интернет-магазина.

В модуле **created_classes** созданы, описаны и инициированы классы *Product* и *Category*, *Smarphone* и *LawnGrass*
а в **utils** - реализовано дополнительное задание: чтение данных из JSON-файла и создание объектов вышеуказанных классов.
В модуле **products_iterator** создан вспомогательный класс, с помощью которого можно перебирать товары одной категории.

Созданы приватные атрибуты цены и списка товаров, геттеры и сеттеры для чтения и изменения приватных атрибутов.

Реализовано тестирование существующих методов классов *Product*, *Category*, *Smarphone*, *LawnGrass*.

От родительского класса *Product* созданы дочерние классы *Smarphone* и *LawnGrass*. Реализовано сложение числа товаров одной категории.

Далее создан базовый класс *BaseProduct* (который является родительским классом для класса *Product*), а также класс *PrintMixin*,
осуществляющий вывод информации об экземплярах классов *Product*, *Smarphone* и *LawnGrass* в консоль.

Покрытие кода проекта тестами согласно *Test Coverage* составляет 95%.
